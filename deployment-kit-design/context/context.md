# Deployment Kit 设计上下文

## 📋 文档目的

本文档提供 Deployment Kit 设计项目的完整上下文，包括：
- 项目背景和目标
- 关键设计决策及其原因
- 架构模式和约定
- 已解决的技术问题
- 待讨论的开放问题
- **架构分析结果**（2026-03-26更新）
- **场景分析结果**（2026-03-26更新）
- 后续工作方向

**用途**：作为项目交流的参考文档，帮助快速理解设计背景和决策依据。

---

## 🎯 项目概述

### 项目定位

**Deployment Kit** 是一个给 **Agent 框架**使用的 **HIS 代码化部署套件**，采用技能化（Skills）的设计理念。

**产品形态：独立套件（非Agent插件）**
- 独立CLI工具（dk）
- 预留Agent集成接口
- 支持多种使用场景（CLI、CI/CD、脚本、Agent）

**核心概念：**
- **HEAM协议**：规范（抽象），定义资源/数据操作的代码化模型
- **XaC代码**：HEAM协议的表现形式（具体），也叫做as code代码

### 核心特点

```
✅ 技能化：将部署流程分解为 20 个独立技能
✅ 可编排：通过编排器灵活组合技能
✅ Agent 友好：项目级数据存储，便于 Agent 使用
✅ 智能化：自动检查、自动修复、智能提示
✅ 套件模式：独立可用的部署套件
✅ 技能插件化：支持扩展自定义技能
```

### 适用场景

**主要用户场景**：
1. **首次 XaC 化**：用户现网有资源但没有 XaC 代码，通过 Deployment Kit 快速生成 XaC 代码
2. **资源更新**：现网资源变化后，更新已有 XaC 代码
3. **自动部署**：代码合入自动触发部署
4. **生产部署**：多环节验证、风险控制的生产环境部署
5. **异常处理**：部署失败分析和快速恢复

**核心用户场景**（2026-03-26更新）：
1. **开发和迭代**：编写XaC代码、本地验证、快速部署
2. **环境管理**：多环境配置、对比、晋升、状态管理
3. **配置管理**：Git集成、配置对比、合并、回滚
4. **生产部署**：演练、灰度、回滚、验证
5. **紧急回滚**：快速响应、分阶段恢复

---

## 🏗️ 架构设计

### 整体架构（分层模型）

**套件模式架构（2026-03-26更新）：**

```
┌─────────────────────────────────────────┐
│         CLI 工具 (dk)                    │
│  命令解析 • 参数验证 • 项目初始化         │
│  前置检查 • 结果展示 • 用户交互           │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│         编排器                           │
│  • 技能编排  • 数据流  • 状态管理        │
│  • 缓存策略  • 错误处理                  │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│         技能层 (20 Skills)               │
│  • 技能插件化  • 工作流库               │
│  • 质量保证体系                         │
└─────────────────┬───────────────────────┘
                  │
                  │ MCP 调用
                  ▼
┌─────────────────────────────────────────┐
│       MCP 服务层                         │
│  • 部署桌面 (XaC生成、资源管理、配置)   │
│  • CF 平台 (计划验证、部署执行)         │
│  • 其他 MCP 服务（状态查询）            │
└─────────────────────────────────────────┘
```

### 核心：技能插件化架构（开闭原则）

**设计理念：稳定骨架 + 插件扩展**

```
核心骨架（稳定，很少修改）：
├── SkillBase 抽象接口
├── Orchestrator 编排器
├── SkillLoader 加载器
└── CacheManager 缓存管理器

插件技能（随意扩展，不影响骨架）：
├── DiscoverResourcesSkill
├── GenerateXacSkill
└── 任何新增技能...

关键：新增技能时，骨架代码完全不需要修改 ✅
```

### 参考：Superpowers 设计模式

```
Superpowers:                    Deployment Kit:
.claude/                    .deployment-kit/
├── memory/                  ├── cache/
│   ├── user_*.md           │   └── {appid}/
│   ├── feedback_*.md        ├── dependencies.json
│   └── MEMORY.md           └── state.json
└── settings.local.json

核心模式：
• 项目级数据存储
• Agent 在工作目录直接访问
• 数据跟随项目移动
• 简化设计，专注 Agent 使用
```

---

## 🎯 关键设计决策

### 0. 产品形态：套件模式（2026-03-26新增）

**决策**：Deployment Kit 作为独立套件，而非Agent插件

**原因**：
- ✅ **专业性**：部署是专业领域，需要深度优化
- ✅ **独立性**：可独立运行，不依赖Agent框架
- ✅ **灵活性**：支持CLI、CI/CD、脚本、Agent多种使用场景
- ✅ **迭代快**：独立版本管理，不受Agent框架约束

**实现**：
- 独立CLI工具（dk）
- 预留Agent插件接口（可选）
- Python SDK（脚本集成）

### 1. 项目级数据存储

**决策**：数据存储在项目目录 `.deployment-kit/`

**原因**：
- ✅ Agent 在项目上下文中工作
- ✅ 数据跟随项目，便于理解
- ✅ 不同项目数据隔离
- ✅ 参考 Superpowers 的 `.claude/` 模式
- ✅ 不需要考虑 git、版本管理（给 Agent 用）

**目录结构**：
```
项目目录/
├── .deployment-kit/
│   ├── cache/
│   │   └── {appid}/
│   │       ├── manifest.json
│   │       ├── resources.json
│   │       └── metadata.json
│   ├── dependencies.json
│   ├── state.json
│   ├── skills/              # 技能插件目录（新增）
│   │   ├── builtin/       # 内置技能
│   │   └── custom/        # 自定义技能
│   └── workflows/           # 工作流定义
└── 项目文件...
```

### 2. 依赖关系管理（2026-03-26更新）

**决策**：人工指定 + 智能建议

**原因**：
- ✅ 准确性：只有人真正了解业务依赖
- ✅ 可控：避免自动推断的错误
- ✅ 可审计：明确的依赖关系便于审查
- ✅ 智能：提供基于HEAM协议的智能建议（待HEAM协议提供后实现）

**实现**：
- 用户编辑 `.deployment-kit/dependencies.json`
- 提供 `dk dependencies edit` 命令
- 提供 `dk dependencies suggest` 命令（智能建议）
- 提供 `dk dependencies validate` 验证
- 提供 `dk dependencies visualize` 可视化

### 3. 混合缓存策略

**决策**：默认使用缓存，支持强制刷新

**原因**：
- ✅ 性能：避免重复调用 MCP
- ✅ 一致性：基于同一份数据生成
- ✅ 灵活性：支持 `--fresh`、`--no-cache` 选项

**缓存增强（2026-03-26更新）**：
- 缓存元数据增加：HEAM版本、Terraform版本、状态哈希
- 版本兼容性检查
- 数据完整性验证

**实现**：
```bash
# 默认：优先缓存
dk generate-xac --appid xxx

# 强制刷新
dk generate-xac --appid xxx --fresh

# 忽略缓存
dk generate-xac --appid xxx --no-cache
```

### 4. 技能编排模式

**决策**：技能分为 generate-xac（全新生成）和 update-xac（增量更新）

**原因**：
- ✅ 明确的语义：用户知道用哪个
- ✅ generate-xac：覆盖模式，用于首次 XaC 化
- ✅ update-xac：保留模式，用于更新现有 XaC

### 5. 边界明确：部署套件 vs 平台能力（2026-03-26新增）

**部署套件负责**：
- ✅ XaC代码生成和管理
- ✅ 部署流程编排
- ✅ 环境管理（对比、升级、状态）
- ✅ 配置管理（Git集成、对比、合并、回滚）
- ✅ 临时验证（部署验证）
- ✅ 环境状态查询

**平台负责**：
- ❌ 持续监控
- ❌ 事件响应
- ❌ 平台治理
- ❌ 告警通知
- ❌ 权限管理

**集成方式**：
- Deployment Kit ← MCP调用 → 平台状态查询接口

---

## 📊 技能分类

### 核心技能（9 个）- 主工作流程

| 技能名称 | 功能描述 | 状态 |
|---------|---------|------|
| discover-resources | 发现现网资源 | 规划中 |
| generate-xac | 生成 XaC 代码 | ✅ 已完成 |
| update-xac | 更新 XaC 代码 | 规划中 |
| validate-syntax | 语法校验 | 规划中 |
| validate-plan | 计划验证 | 规划中（CF平台提供） |
| test-deploy | 测试环境部署 | 规划中（CF平台提供） |
| deploy-production | 生产环境部署 | 规划中（CF平台提供） |
| analyze-failure | 分析部署失败 | 规划中 |
| evaluate-canary | 评估灰度效果 | 规划中 |

### 质量技能（4 个）

| 技能名称 | 功能描述 | 状态 |
|---------|---------|------|
| review-code | XaC代码审查 | 规划中 |
| check-compliance | 合规检查 | 规划中 |
| monitor-deployment | 部署监控 | 规划中 |
| monitor-resources | 资源监控 | 规划中 |

### 管理技能（2 个）

| 技能名称 | 功能描述 | 状态 |
|---------|---------|------|
| manage-config | 配置管理 | 规划中 |
| manage-version | 版本管理 | 规划中 |

### 应急技能（5 个）

| 技能名称 | 功能描述 | 状态 |
|---------|---------|------|
| diagnose-error | 错误诊断 | 规划中 |
| auto-fix | 自动修复 | 规划中 |
| rollback-deployment | 快速回滚 | 规划中 |
| dry-run-rehearsal | 生产演练 | 规划中 |
| deploy-canary | 灰度部署 | 规划中 |

---

## 🔧 MCP 服务能力

### 已知 MCP 服务

#### 1. 资源管理 MCP 服务（部署桌面）

**能力**：
- 根据 `appid` 获取应用下的资源统计
- 调用各个 tools 获取详细资源数据
- 每种资源类型有对应的 tool

**Tools**：
- `get_resource_stats`: 获取资源统计
- `get_ads_list`: 获取 ADS 资源列表
- `get_config_list`: 获取配置资源列表
- `get_...`: 其他资源类型

**能力边界**：
- ✅ 提供资源统计数据
- ✅ 提供详细资源数据
- ❌ 不提供依赖关系
- ❌ 不提供 DAG 生成

#### 2. XaC 编排 MCP 服务（CF 平台）

**能力**：
- `validate-plan`: 计划验证
- `deploy-production`: 生产部署
- `test-deploy`: 测试部署

**能力边界**：
- ✅ 计划验证和风险评估
- ✅ 部署执行
- ❌ 不提供资源发现
- ❌ 不提供 XaC 代码生成

#### 3. 状态查询 MCP 服务（平台）（2026-03-26新增）

**能力**：
- `query_environment_status`: 查询环境状态
- `query_deployment_status`: 查询部署状态
- `query_resource_health`: 查询资源健康状态

**能力边界**：
- ✅ 提供状态查询接口
- ❌ 不提供持续监控
- ❌ 不提供告警通知

### MCP 服务集成方式

```
技能层调用 MCP 服务：
├── 部署桌面 MCP 服务
│   ├── discover-resources（资源发现）
│   ├── generate-xac（XaC 代码生成）
│   └── manage-config（配置管理）
│
└── CF 平台 MCP 服务
    ├── validate-plan（计划验证）
    └── deploy-production（部署执行）

集成方式：
Deployment Kit ← 并发MCP调用 → 平台服务
- 支持并发调用提升性能
- 支持重试机制提升可靠性
- 支持降级策略提升容错性
```

---

## 💾 数据流和缓存

### 数据流

```
用户请求 → CLI 工具 → 编排器 → 技能层 → MCP 服务层
    ↓          ↓        ↓       ↓        ↓
  用户目录  .deployment-kit  技能逻辑  网络调用
    ↓          ↓        ↓       ↓        ↓
  state.json  cache/   处理逻辑  返回数据
              ↓       ↓
          生成结果  技能输出
```

### 缓存策略

```
discover-resources:
  调用 MCP → 保存到 .deployment-kit/cache/{appid}/
  缓存内容：manifest.json, resources.json, metadata.json
  缓存元数据：HEAM版本、Terraform版本、状态哈希

generate-xac:
  读取缓存（默认）→ 生成 XaC 代码 → 保存到 xac/
  支持模式：cache（默认）、fresh、no-cache

validate-plan:
  调用 CF 平台 MCP → 验证计划 → 返回验证结果
  不使用缓存（总是实时）
```

---

## 🎯 核心使用场景

### 场景 1：首次 XaC 化

```
用户：现网有资源，从未写过 XaC 代码

流程：
1. dk discover --appid xxx
   → 调用 MCP 获取资源
   → 保存到缓存
   → 提示编辑依赖关系

2. dk dependencies edit --appid xxx
   → 编辑依赖关系文件

3. dk generate-xac --appid xxx
   → 从缓存读取资源数据
   → 读取依赖关系
   → 生成 XaC 代码

4. dk validate-syntax
   → 验证生成的代码

5. dk validate-plan --appid xxx
   → 调用 CF 平台验证

6. dk deploy-production --appid xxx
   → 调用 CF 平台部署
```

### 场景 2：资源更新

```
用户：现网资源有变化，需要更新 XaC

流程：
1. dk discover --appid xxx --refresh
   → 刷新资源数据

2. dk generate-xac --appid xxx
   → 重新生成 XaC 代码（覆盖）

3. dk validate-syntax
   → 验证代码

4. dk deploy-production
   → 部署
```

### 场景 3：快速部署

```
用户：一键部署，自动化

流程：
dk quick-deploy --appid xxx
→ 自动执行完整流程
→ 包含所有检查和验证
```

---

## 🔓 架构分析结果（2026-03-26）

### 整体架构评分：**7.5/10** ⭐⭐⭐⭐

### 发现的架构问题（12个）

| # | 问题 | 类型 | 优先级 | 状态 |
|---|------|------|--------|------|
| **1** | 入口层与编排器职责重叠 | 架构层次 | P0 | 已分析 |
| **2** | 缓存策略缺少版本管理 | 数据存储 | P0 | 已分析 |
| **3** | 依赖关系未充分利用HEAM | 设计合理性 | P0 | 已分析 |
| **4** | HEAM协议定位不够明确 | 技术栈对齐 | P0 | 已分析 |
| **5** | validate-plan职责不清 | 职责边界 | P1 | 已分析 |
| **6** | MCP调用缺少并发和重试 | 可靠性 | P0 | 已分析 |
| **7** | MCP服务版本兼容性 | 可靠性 | P1 | 已分析 |
| **8** | 套件模式下架构层次冗余 | 架构层次 | P0 | 已分析 |
| **9** | CLI与编排器接口不清晰 | 接口设计 | P0 | 已分析 |
| **10** | 技能系统不支持插件化 | 可扩展性 | P1 | 已分析 |
| **11** | 工作流缺少复杂编排能力 | 功能完整性 | P1 | 已分析 |
| **12** | 数据存储缺少版本控制 | 数据存储 | P1 | 已分析 |

### 核心建议：技能插件化架构

**设计理念：稳定骨架 + 插件扩展**

```
核心骨架（稳定，很少修改）：
├── SkillBase 抽象接口
├── Orchestrator 编排器
├── SkillLoader 加载器
└── CacheManager 缓存管理器

插件技能（随意扩展，不影响骨架）：
├── DiscoverResourcesSkill
├── GenerateXacSkill
└── 任何新增技能...

关键：新增技能时，骨架代码完全不需要修改 ✅
```

### 设计原则

1. **依赖抽象，不依赖具体** - 编排器只依赖 SkillBase 接口
2. **技能自我声明** - 通过属性声明自己的特性
3. **统一数据流** - 所有技能使用相同的输入输出机制
4. **单一职责** - 每个组件只做一件事

---

## 🎯 场景分析结果（2026-03-26）

### 场景覆盖率评估：**55%** ⭐⭐⭐

### 发现的场景问题（12个）

| # | 问题 | 影响的用户 | 优先级 |
|---|------|-----------|--------|
| **S1** | 缺少"从零创建应用"的场景 | 开发者 | P0 |
| **S2** | 缺少"本地验证XaC"的场景 | 开发者 | P0 |
| **S3** | 缺少"开发迭代"的场景 | 开发者 | P0 |
| **S4** | 缺少"实时监控"的场景（已明确为平台能力） | - | - |
| **S5** | 缺少"问题诊断"的场景（已明确为平台能力） | - | - |
| **S6** | 缺少"配置管理"的场景 | 运维/开发者 | P0 |
| **S7** | 缺少"代码审查审批"的场景 | 管理员 | - |
| **S8** | 缺少"多环境管理"的场景 | 开发者/运维 | P0 |
| **S9** | 缺少"配置漂移检测"的场景 | 运维 | P1 |
| **S10** | 缺少"快速应急恢复"的场景 | 运维 | P0 |
| **S11** | 缺少"版本升级"的场景 | 开发者 | P1 |
| **S12** | 缺少"多环境自动晋升"的场景 | 开发者 | P1 |

### 核心场景（2026-03-26更新）

#### 场景1：开发和迭代（P0）

**用户意图：** "我需要修改应用配置，重新部署"

**流程：**
1. edit-xac-code（编辑XaC代码）
2. validate-xac-locally（本地验证）
3. deploy-to-test（部署到测试环境）
4. verify-deployment（验证部署结果）
5. iterate-if-needed（如果需要则迭代）

#### 场景2：环境管理（P0）

**用户意图：** "我需要管理开发、测试、生产3个环境"

**能力包括：**
- 基础能力：生成Specs、部署、切换
- 对比能力：配置对比、状态对比
- 升级能力：环境间晋升
- 状态管理：环境状态跟踪

**不包括：** 权限管理

#### 场景3：配置管理（P0）

**用户倾向：** 高级管理（Git集成、对比、合并、回滚）

**流程：**
1. Git集成（版本控制）
2. 配置对比（差异分析）
3. 配置合并（冲突解决）
4. 配置回滚（版本恢复）

#### 场景4：生产部署（P0）

**流程：**
1. 生产演练（预演）
2. 灰度发布（逐步放量）
3. 全量发布
4. 部署验证（临时监控）

#### 场景5：紧急回滚（P0）

**分阶段响应：**
- Phase 1: 快速诊断（0-5分钟）
- Phase 2: 快速恢复（5-15分钟）
- Phase 3: 根本分析（15分钟后）

### 环境管理与监控打通

**核心理解：**

```
监控 = 环境状态管理的一部分

Deployment Kit 提供：
├── 查询环境状态（临时）
└── 验证部署结果（临时）

平台提供：
├── 持续监控
├── 告警通知
└── 监控仪表板

打通方式：
Deployment Kit ←MCP调用→ 平台状态查询接口
```

---

## ❓ 待讨论的开放问题

### 问题 1：HEAM协议的具体规范（2026-03-26更新）

**背景**：XaC代码是HEAM协议的表现形式

**需要讨论**：
1. HEAM协议的资源类型映射表
2. HEAM协议的属性映射规则
3. HEAM协议的数据类型转换规则
4. 特殊资源处理规则
5. 最佳实践（命名规范、注释规范）

**当前状态**：等待HEAM协议规范提供

### 问题 2：转换规则的具体设计

**背景**：`generate-xac` 的核心是资源 → XaC 的转换规则

**需要讨论**：
1. 资源类型映射表（HIS 资源 → XaC 资源）
2. 属性映射规则（HIS 属性 → XaC 参数）
3. 数据类型转换规则
4. 特殊资源处理（复杂对象、嵌套结构）
5. 最佳实践（命名规范、注释规范）

**当前状态**：已确定需要转换规则，具体规则待HEAM协议提供后讨论

### 问题 3：update-xac 的实现细节

**背景**：`update-xac` 需要保留用户自定义内容

**需要讨论**：
1. 如何识别用户自定义内容？
2. 如何保留用户自定义？
3. 如何合并新生成的内容？
4. 差异报告如何生成？

**当前状态**：概念已确定，实现细节待讨论

### 问题 4：validate-plan 的具体流程

**背景**：`validate-plan` 调用 CF 平台 MCP 服务

**需要讨论**：
1. MCP 服务调用协议
2. 计划结果解析
3. 风险评估标准
4. 用户如何查看和确认计划？

**当前状态**：知道调用 CF 平台，具体协议待讨论

### 问题 5：技能插件化的实现细节

**背景**：实现技能插件化，保证骨架稳定性

**需要讨论**：
1. SkillBase 抽象接口的具体设计
2. 技能加载机制
3. 技能版本管理
4. 技能依赖解析

**当前状态**：架构设计完成，实现细节待讨论

---

## 🚀 后续工作方向（2026-03-26更新）

### P0（立即行动）

**架构层面：**
1. ✅ 确定套件模式
2. 实现技能插件化骨架
3. 修复P0架构问题（7个）
4. 明确HEAM协议地位

**场景层面：**
1. 实现开发迭代场景
2. 实现环境管理场景
3. 实现配置管理场景
4. 实现生产部署场景

### P1（近期优化）

1. 增强工作流编排能力
2. 实现技能插件化
3. 增强MCP调用可靠性
4. 缓存版本管理

### P2（长期演进）

1. Agent集成接口
2. 技能生态建设
3. Web UI（可选）

---

## 📚 关键文档索引

### 设计文档

| 文档名称 | 描述 | 位置 |
|---------|------|------|
| 00-架构设计图.md | 整体架构概览 | deployment-kit-design/ |
| 01-overview.md | 概述和背景 | deployment-kit-design/ |
| 02-skills-definition.md | 20 个技能详细定义 | deployment-kit-design/ |
| 03-skill-relationships.md | 技能关系和依赖 | deployment-kit-design/ |
| 04-workflows.md | 预定义工作流 | deployment-kit-design/ |
| 05-orchestrator-design.md | 编排器设计 | deployment-kit-design/ |
| 06-evolution-mechanism.md | 演进机制 | deployment-kit-design/ |
| 08-data-storage-and-dependencies.md | 数据存储和依赖管理 | deployment-kit-design/ |
| 09-entry-and-orchestration.md | 入口层和编排器集成 | deployment-kit-design/ |

### 分析文档（2026-03-26新增）

| 文档名称 | 描述 | 位置 |
|---------|------|------|
| 架构分析报告 | 架构问题分析和建议 | deployment-kit-design/context/ |
| 场景分析报告 | 场景问题分析和建议 | deployment-kit-design/context/ |

### 上下文文档

| 文档名称 | 描述 | 位置 |
|---------|------|------|
| context.md | 本文档：项目上下文 | deployment-kit-design/context/ |

---

## 🔄 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| 1.0.0 | 2026-03-26 | 初始版本 | Deployment Kit 设计团队 |
| 1.1.0 | 2026-03-26 | 架构分析结果、场景分析结果、产品形态决策 | Deployment Kit 设计团队 |

---

## 💡 如何使用本文档

### 快速了解项目

1. **新加入团队成员**：阅读"项目概述"和"整体架构"
2. **技术实现**：阅读"关键设计决策"和"架构设计"
3. **具体问题**：查看"架构分析结果"和"场景分析结果"
4. **继续工作**：查看"后续工作方向"

### 与团队讨论

1. **讨论技术方案**：引用"关键设计决策"中的原因
2. **提出新问题**：添加到"待讨论的开放问题"
3. **更新上下文**：及时更新"变更记录"

### 后续参考

1. **查看约定**：使用"重要约定和规范"
2. **查找文档**：使用"关键文档索引"
3. **了解进展**：查看"后续工作方向"

---

## 📞 联系方式

- **项目名称**：Deployment Kit
- **创建日期**：2026-03-26
- **文档维护**：Deployment Kit 设计团队
- **文档位置**：deployment-kit-design/context/context.md

---

**提示**：本文档是活文档，随着项目进展持续更新。每次重要决策后请及时更新本文档。
